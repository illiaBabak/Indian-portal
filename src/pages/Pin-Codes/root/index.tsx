import { Header } from '../components/Header';
import { PinCodesList } from '../components/PinCodesList';

export const PinCodes = (): JSX.Element => {
  return (
    <div>
      <Header />
      <PinCodesList />
    </div>
  );
};
